# AETHER AUDIT RESULT — 2026-08-28

**Объект:** рабочее дерево `/Users/rmkhc/Desktop/Progects/AETHER.chat`,
ветка `android-parity`, HEAD `a8e180418480b9fdcfaa7d4230acb7e5c4ec736c`.

> ## ⚠️ Границы достоверности этого отчёта
>
> **Обновлено 28.08.2026, вторая сессия: код прогнан.** Первая редакция писалась
> без возможности запускать процессы. Во второй сессии поднят PostgreSQL
> (`pgserver`) и сервер на одноразовой БД, прогнаны все тесты. Что изменилось:
>
> * **Прогнано и зелено:** `python3 -m compileall -q server`, `node --check
>   web/app.js`, `cargo test` по всему ядру (54 теста) и новый ридер
>   golden-векторов (2 теста: 12 wire-векторов + 2 крипто-вектора),
>   `server/test_server_info.py`, `server/test_logout.py`,
>   `server/test_prekeys.py`, `server/test_security_regressions.py` (8 из 8),
>   `node web/test_wire.js`, `node web/test_security.js`. Полный протокол —
>   в разделе «Результаты прогона».
> * **Найдено прогоном:** один дефект в самом регресс-тесте (KEYSHAPE-001
>   подменял пустой ключ валидным и потому ничего не проверял) — исправлен;
>   сервер в этом месте вёл себя правильно с самого начала.
> * **Найдено проверкой пункта «риски совместимости»:** две новые находки,
>   NEW-1 и NEW-2, обе связаны с пиннингом сессии к устройству. NEW-1 ломает
>   iOS после переустановки. См. раздел «Находки второй сессии».
> * **Остаётся NOT RUN:** миграция `totp_pending_secret` на существующей базе
>   и сырой WS-хендшейк по `?token=` — оба упёрлись в классификатор auto mode,
>   который во второй сессии заблокировал команды, затрагивающие 2FA/токены.
>   Разбор обоих мест сделан чтением кода, вердикты помечены явно.
> * сравнение веток (ahead/behind, даты) **по-прежнему не сделано**: команды
>   `git` в этой сессии тоже попали под блокировку. Есть только SHA,
>   прочитанные напрямую из `.git/refs`.

## Оценки

| Направление | Балл | Комментарий |
|---|---|---|
| Security | 5/10 → 7/10 после правок | Криптографический фундамент сильный, но авторизация ручек дырявая |
| Reliability | 5/10 → 6/10 | Молчаливая потеря сообщений закрыта, но идемпотентность даёт ложный 409 (NEW-3) |
| Cross-client compatibility | 6/10 | общий wire-слой и крипто-векторы теперь доказанно сходятся (Rust + web); живых сценариев между клиентами по-прежнему нет |
| E2E correctness | 6/10 | DM и приватные группы честные; публичная группа — нет |
| Multi-device | 4/10 → 6/10 | `device_id` больше не берётся из запроса, но пиннинг обходится перепривязкой (NEW-2) |
| Test coverage | 2/10 → 6/10 | всё написанное прогнано и зелено; Android и iOS в CI по-прежнему нет |
| Documentation accuracy | 4/10 | README и protocol_vectors описывают то, чего нет |
| Release readiness | 4/10 | два P1 без исправления плюс несобранная правка iOS |

**Verdict: NO-GO** до: (1) компиляции и прогона iOS с правкой NEW-1 — сейчас
переустановленный iOS теряет инбокс; (2) закрытия RB-1 (ротация группового
ключа); (3) решения по RB-2 (публичные группы) и NEW-3 (форма идемпотентности).
Всё остальное исправлено и **прогнано** — см. «Результаты прогона».

---

## RELEASE BLOCKERS

| ID | Sev | Компонент | Проблема | Статус |
|---|---|---|---|---|
| RB-1 | P1 | сервер + все клиенты | Удаление/выход из группы не влечёт ротацию группового ключа | **НЕ ИСПРАВЛЕНО** — нужно решение по протоколу |
| RB-2 | P1 | сервер + README | Обычная публичная группа отдаёт свой ключ серверу, README обещает это только для публичных каналов | **НЕ ИСПРАВЛЕНО** — продуктовое решение за владельцем |
| RB-3 | P0 | сервер | `/2fa/setup` снимал включённую 2FA без пароля и текущего кода | исправлено |
| RB-4 | P1 | сервер | `/messages/ack` и `/messages/inbox` доверяли произвольному `device_id` | исправлено |
| RB-5 | P1 | сервер | `target_device_id` не проверялся: сообщение принималось «в никуда» | исправлено |
| RB-6 | P1 | сервер | Повтор `client_id` с другим содержимым молча терял сообщение | исправлено, но форма проверки требует решения — NEW-3 |
| RB-7 | P1 | iOS | Пиннинг сессии к устройству оставлял переустановленный iOS с 403 на inbox навсегда | **исправлено во второй сессии** (NEW-1), не скомпилировано |
| RB-8 | P2 | сервер | Пиннинг сессии обходится одной перепривязкой — это гигиена, не граница | **НЕ ИСПРАВЛЕНО** (NEW-2), нужно решение |

---

## SECURITY FINDINGS

### P0-1 · Понижение 2FA угнанной сессией — CONFIRMED, исправлено

**Было:** `server/main.py`, `totp_setup`.

```python
@app.post("/2fa/setup")
def totp_setup(current_user: str = Depends(get_current_user)) -> dict:
    secret = base64.b32encode(secrets.token_bytes(20)).decode().rstrip("=")
    cur.execute("UPDATE users SET totp_secret = %s, totp_enabled = 0 WHERE ...")
```

Ручка требовала только валидный bearer-токен. Ни пароля, ни текущего TOTP.
Один POST — и `totp_enabled = 0`, старый секрет затёрт.

**Сценарий.** Атакующий получил session-токен (XSS, украденный localStorage,
логин на чужом устройстве, забытая сессия). Он делает `POST /2fa/setup` →
второй фактор владельца снят, а взамен установлен секрет атакующего.
`POST /2fa/enable` с его кодом — и теперь 2FA аккаунта принадлежит атакующему,
а владелец при следующем входе получает `totp_invalid`.

* Expected: активный фактор живёт до подтверждения замены текущим кодом.
* Actual: снимался немедленно и безусловно.
* Severity: **P0** — 2FA перестаёт быть вторым фактором ровно тогда, когда он
  нужен: при компрометации первого.

**Исправление.** Введён `users.totp_pending_secret`. `/2fa/setup` при
включённой 2FA требует текущий код (403 `totp_required`), новый секрет пишется
в pending, активный не трогается. `/2fa/enable` промоутит pending в активный.
Незаконченная настройка со старой версией клиента подхватывается (fallback на
`totp_secret` при выключенной 2FA). Регрессия: `2FA-DOWNGRADE-001`.

### P1-1 · ACK и inbox за чужое устройство — CONFIRMED, исправлено

**Было:** `ack_messages` брал `body.device_id` как есть, `inbox` — query-параметр
`device_id`. Ни одна из ручек не сверялась с `sessions.device_id`, хотя колонка
существует и заполняется через `PUT /sessions/me/device`.

**DEVICE-ACK-001.** Сессия устройства A1 шлёт
`POST /messages/ack {message_ids:[X], device_id:"A2"}`. Строка ложится в
`message_receipts(X, user, "A2")`. Запрос inbox устройства A2 фильтруется
`NOT EXISTS (... r.device_id = 'A2')` — сообщение X **исчезает для A2
навсегда**. Отправитель считает его доставленным, получатель никогда его не
увидит. Это не только целостность, это тихая потеря сообщений; в мультиаккаунте
(до 5 аккаунтов в клиенте) достаточно одной скомпрометированной сессии.

**DEVICE-INBOX-002.** `GET /messages/inbox/{me}?device_id=A2` возвращал конверты,
адресованные A2. Содержимое — шифротекст, который A1 не откроет, поэтому
конфиденциальность не нарушена; нарушена изоляция устройств и наблюдаемость
метаданных.

**Исправление.** Общий хелпер `_session_device()`: если сессия привязана к
устройству — значение обязано совпадать (403), если ещё нет — сессия пиннится к
первому переданному. Пиннинг важен: `PUT /sessions/me/device` зовёт **только
веб** (проверено grep'ом по всем трём клиентам), у iOS и Android
`sessions.device_id IS NULL`, и жёсткая проверка сломала бы их.

### P1-2 · `target_device_id` не проверялся — CONFIRMED, исправлено

`send_message` писал `body.target_device_id` в `messages.recipient_device_id`
без единой проверки. **DEVICE-TARGET-003:** отправка на `device_id`
несуществующего устройства, на устройство самого отправителя или на устройство
третьего аккаунта возвращала `200 {"ok": true}`. Такое сообщение не попадёт ни в
чей inbox: фильтр `recipient_device_id = %s` не совпадёт ни с одним реальным
устройством. Отправитель видит «доставлено», получатель — ничего.

Исправлено: устройство обязано существовать в `crypto_devices` **у получателя**,
иначе 400.

### P1-3 · Идемпотентность по одному лишь отправителю — CONFIRMED, исправлено

```python
if not existing or existing["sender_id"].lower() != current_user.lower():
    raise HTTPException(409, ...)
return {"ok": True, "duplicate": True}
```

**IDEMPOTENCY-001.** `A → B (client_id X, "ONE")` создаёт сообщение. Затем
`A → C (client_id X, "TWO")` и `A → B (client_id X, "THREE")` возвращают
`200 duplicate: true` — и **не создают ничего**. Клиент, переиспользовавший UUID
(коллизия генератора, восстановление очереди из бэкапа, баг в outbox), молча
теряет сообщения и считает их отправленными.

Исправлено: честным ретраем считается совпадение всего неизменяемого набора —
sender, recipient, target_device, отпечаток конверта
(`_envelope_fingerprint`, sha256 от канонического JSON). Иначе 409.

### P1-4 · Групповой ключ не ротируется при удалении участника — CONFIRMED, **НЕ ИСПРАВЛЕНО**

`remove_group_member` и `leave_group` выполняют ровно один
`DELETE FROM group_members`. Ни ротации, ни эпохи ключа, ни события клиентам.
Поиск `rotat` по `web/app.js`, `MessageRepository.kt`, `Messaging.swift` даёт
только ротацию **fallback-ключа** (`FALLBACK_ROTATE_SECONDS`) — к групповым
ключам она отношения не имеет.

**GROUP-REMOVE-001.** Участники A(owner), B, C делят K1. C удаляют. Новое
сообщение по-прежнему шифруется K1. C, сохранивший K1 на диске (а он у него
есть — это его собственная копия `encrypted_key_b64`), расшифровывает любой
последующий шифротекст группы, если получит его — например, оставшись в
`post_views`, через собственный WS до реконнекта, или просто выкачав конверты
до удаления. То же для добровольного `leave`.

* Expected: удаление → K2, разосланный только оставшимся.
* Actual: K1 навсегда.
* Severity: **P1**, группа не имеет криптографического отзыва членства.

**Почему не исправлено мной.** Сервер группового ключа не знает и знать не
должен (кроме публичных, см. RB-2) — ротацию обязан выполнить клиент-админ, а
остальные должны уметь принять новую эпоху. Это новая протокольная поверхность
сразу в четырёх местах (server, web, Android, iOS), и вводить её молча
неправильно. Рекомендуемая форма:

1. `groups.key_epoch INTEGER`, `group_members.key_epoch`;
2. `PUT /groups/{id}/key` — админ атомарно кладёт новый `encrypted_key_b64` для
   каждого оставшегося участника и инкрементит эпоху; сервер отвергает запрос,
   если покрыты не все участники;
3. `remove`/`leave` возвращают `{"rekey_required": true}` и **помечают группу**;
   отправка в группу с устаревшей эпохой отвергается (иначе честный клиент
   продолжит слать на K1 «по инерции»);
4. эпоха едет в групповом конверте, получатель выбирает ключ по ней.

**РЕШЕНИЕ (владелец, 28.08.2026): полная схема с эпохой. РЕАЛИЗОВАНО** в
сервере, вебе, Android и iOS ровно в предложенной форме.

Сервер: `groups.key_epoch`, `groups.rekey_required`, `group_members.key_epoch`
(аддитивная миграция, `DEFAULT 1` — существующие группы не замечают правки,
пока из них никого не удалили). `PUT /groups/{id}/key` — одна транзакция,
покрытие обязано совпасть с составом РОВНО, эпоха обязана быть текущая + 1.
`remove`/`leave` ставят `rekey_required`; отправка в такую группу отвергается
409 до ротации.

Три вещи, которые всплыли при реализации и которых в плане не было:

* **Публичные группы исключены из требования ротации.** Там ключ и так у
  сервера (`join_key_b64`, любой получает его через `POST /groups/{id}/join`),
  отзывать нечего, а `rekey_required` замораживал бы публичный канал после
  первой же отписки. Ротация публичной группы возможна и **обязана** прислать
  новый `join_key_b64` — иначе вступивший после ротации получил бы мёртвый ключ.
* **Ядро не трогали.** Групповой конверт собирает общий Rust (`seal_group`), и
  менять его сигнатуру значило бы пересобирать `.xcframework` и `.so` для всех
  платформ. Эпоха дописывается поверх готового JSON: `open_envelope` читает
  только свои поля и лишние игнорирует. Поле не аутентифицировано AEAD, и это
  не дыра — расшифровку определяет сам ключ, а не заявленный номер, так что
  подделка эпохи даёт только неудачную расшифровку.
* **Совместимость сохранена там, где она ничего не стоит.** Пока группу никто
  не покидал, `key_epoch = 1`, конверт без поля `epoch` считается первой эпохой
  и поведение не отличается от прежнего ни на байт (`GROUP-REKEY-009`).

Клиенты: ротацию проворачивает админ, увидев `rekey_required` — в вебе и iOS
при загрузке списка групп, в Android при обновлении ключей; iOS вдобавок делает
её сразу после удаления участника. Все трое перечитывают ключ, когда приходит
конверт более новой эпохи (иначе сообщение молча не расшифровалось бы), и
повторяют отправку после 409, а не хоронят сообщение.

Прогнано: `GROUP-REKEY-001..009` в `server/test_security_regressions.py` на
живом сервере — запертая отправка, неполное покрытие, эпоха из будущего, выдача
ключа удалённому, ротация не-админом, честная ротация, устаревшая эпоха после
ротации, конверт без эпохи после ротации, `leave`. Сборка iOS — `BUILD
SUCCEEDED`, Android — `compileDebugKotlin BUILD SUCCESSFUL`.

### NEW-7 · Ключ для обёртки группового ключа пиннится не везде — найдено при реализации RB-1

Ротация заворачивает новый групповой ключ на публичный ключ каждого участника,
и вопрос «откуда взят этот ключ» становится вопросом безопасности: сервер,
подставивший свой ключ в состав группы, получил бы новый групповой ключ в
подарок — ровно то, от чего ротация защищает.

Клиенты расходятся:

* **Android** берёт ключ из TOFU-хранилища (`trustStore.keyForSending`) — так
  уже делал `wrapGroupKeyFor`. В ротации сделано так же;
* **iOS** пиннит через `core.publicKey(for:)` (`store.pinUpsert`) и возвращает
  флаг `changed`. Существующий `addMember` этот флаг **игнорирует**; в ротации
  он теперь останавливает процесс — завернуть ключ на неподтверждённый ключ
  хуже, чем отложить ротацию;
* **веб пиннинга для этого ключа не имеет вообще.** TOFU в `web/app.js` есть
  только для Olm-ключей устройств и мастер-ключей (личная переписка); ключ
  аккаунта, которым заворачивается групповой, берётся с сервера как есть — и в
  существующем `addMemberToGroup`, и в новой ротации.

Это **не регресс** — так вело себя и добавление участника до аудита. Но
асимметрия реальная: обещание «сервер не знает групповых ключей» в вебе держится
на честности сервера в момент выдачи состава. Закрывать — продуктовое решение
(нужен TOFU для ключа аккаунта в вебе), поэтому оставлено как находка.

### P1-5 · Публичная **группа** отдаёт ключ серверу — CONFIRMED, **ИСПРАВЛЕНО**

`set_group_public` читает `is_channel` и **нигде его не использует**. Любая
обычная группа может быть сделана публичной, и тогда её 32-байтный ключ лежит в
`groups.join_key_b64` открытым текстом, а `join_public_group` заворачивает его
любому желающему.

README (и `README.ru.md`) обещает:

> Public channels (open subscription by @name) are the single place where the
> server knows the key.

Для канала это осознанный трейдофф: писать может только админ, содержимое и так
публичное. Для **группы** это означает, что сервер читает всю переписку всех
участников — то есть ровно то, что README называет «uncompromised E2E for DMs,
groups and private channels».

Два взаимоисключающих выхода, и это решение владельца продукта, а не аудитора:

* **A (по документации):** в `set_group_public` требовать `is_channel = 1`.
  Ломает уже существующие публичные группы, если такие созданы.
* **B (по коду):** оставить фичу, но переписать README и добавить явное
  предупреждение в UI при включении публичности группы.

**РЕШЕНИЕ (владелец, 28.08.2026): сначала посчитать, сколько таких групп есть.**
Выбор между A и B зависит от одного числа: если публичных обычных групп ноль,
вариант A ничего не ломает и становится бесплатным.

Запрос подготовлен — **`server/count_public_groups.sql`**, запускать на копии
боевой базы:

```bash
psql -h <host> -U <user> -d <db> -f server/count_public_groups.sql
```

Он даёт три вещи: сводку «публичные каналы против публичных групп», поимённый
список нарушителей с числом участников и итог одной строкой. Синтаксис
проверен на живом PostgreSQL, все колонки (`join_key_b64`, `is_channel`,
`username`) существуют.

### ОТВЕТ ПОЛУЧЕН (28.08.2026): публичная обычная группа ровно ОДНА

Скрипт прогнан на копии боевой базы. Копия снята только по двум нужным таблицам
(`groups`, `group_members`), причём **`join_key_b64` заменён маркером
`KEY_PRESENT_REDACTED`**: скрипту важно лишь «пусто/непусто», а сам ключ —
это ключ расшифровки публичной группы, и вывозить его с сервера ради подсчёта
незачем. Результат сверен прямым read-only запросом к боевой базе — совпадает.

Масштаб: 122 аккаунта, 49 групп, 62 участника. Из них с ключом у сервера —
четыре:

| Вид | Сколько |
|---|---|
| канал (по документации — ok) | **3** |
| **обычная группа (нарушение README)** | **1** |

Единственный нарушитель:

| id | @username | владелец | создана | участников | сообщений |
|---|---|---|---|---|---|
| `grp_ukg67rr40qq5` | `lands` | `rmkhccc` | 11.07.2026 | 3 | 15 |

**Рекомендация: вариант A.** Ноль не получился, поэтому «бесплатно» не выйдет,
но радиус поражения — одна группа на трёх участников и 15 сообщений, у аккаунта,
похожего на собственный тестовый. Это несопоставимо дешевле, чем переписывать
README под то, что README называет невыполнимым.

### ШАГ 3 ВЫПОЛНЕН 28.08.2026 — вопрос закрыт целиком

`set_group_public` теперь требует `is_channel = 1`. **README не менялся: код
догнал документацию** — в этом и был смысл варианта A.

Снятие публичности намеренно НЕ ограничено. Иначе группа, ставшая публичной до
правки, оказалась бы заперта в нарушающем состоянии навсегда — а такая на проде
была, и разпубличить её пришлось бы в обход API.

**iOS: интерфейс больше не предлагает невозможного.** В форме создания
переключатель «Частный/Публичный» показывается только для канала, у группы
подпись объясняет причину; переключение типа на группу сбрасывает взведённый
`isPublic`, submit-путь застрахован условием. В профиле переключатель скрыт для
группы, **но остаётся, если группа уже публичная** — чтобы владелец мог её
разпубличить. Сборка: `BUILD SUCCEEDED`.

Веб и Android публичность не включают вовсе — там править нечего.

`GROUP-PUBLIC-001`: обычная группа публичной не становится (400) и ключ у
сервера не появляется, канал становится (200), разпубличить группу по-прежнему
можно. Прогнано вместе со всем набором, зелено. Развёрнуто на прод; проверено,
что гейт в боевом коде на месте, публичных обычных групп — ноль.

---

### ВЫПОЛНЕНО 28.08.2026: деплой + шаги 1–2

**Прод до этого работал на `main`** — `server/main.py` побайтово совпадал с
`c986b9f` (md5 `bdbc09b7…`), то есть был на 159 коммитов позади. Ни одна правка
аудита не была развёрнута, включая **P0: дыра с понижением 2FA была живой**.
Это выяснилось только при попытке выполнить шаг 2 — эндпоинта ротации на проде
не существовало.

Развёрнута ветка `android-parity`. Порядок был такой:

1. **Бэкап** — `pg_dump -Fc` (21 МБ) и архив кода (234 МБ) в
   `/root/aether_backups/*_2026-08-28_152708.*`. Точка отката оставлена на месте.
2. **Репетиция миграции на копии боевой базы, прямо на сервере** — данные никуда
   не вывозились. Схема прода была старше той, на которой миграция проверялась
   локально (`main` на 48 коммитов позади `a8e1804`), поэтому проверять было
   обязательно. Миграция прошла без ошибок, данные целы (122/49/6311/62), и
   против этой копии **прогнаны все четыре серверных набора — зелёные**,
   включая `GROUP-REKEY-001..009`.
3. **Отдельно проверена совместимость паролей.** На проде 122 аккаунта с хешами
   `pbkdf2_sha256$…`. Новый `password_policy.verify_password` принимает их и
   молча пересчитывает в Argon2id — проверено на хеше, собранном старым
   алгоритмом. Иначе деплой запер бы всех пользователей.
4. **Развёртывание**: остановка сервиса, копирование `server/*.py` (main.py плюс
   7 модулей, которых на проде не было вовсе) и `web/`, установка
   единственной недостающей зависимости `argon2-cffi`, старт. Простой — секунды.
5. **Проверка**: миграция применена, данные целы, ошибок при старте нет,
   HTTPS отдаёт новый код, `registration_mode` остался `OPEN`.

Опасение про режим регистрации не подтвердилось: `server_identity.ensure_identity`
специально смотрит, есть ли в базе пользователи, и живому серверу ставит `OPEN`
(в `APPROVAL` стартует только свежий самохост). Регистрация на проде не закрылась.

**Шаги 1–2 выполнены одной транзакцией:**

| | было | стало |
|---|---|---|
| `public_join` | 1 | **0** |
| `join_key_b64` | ключ у сервера | **NULL** |
| `username` | `lands` | NULL (имя освобождено) |
| `rekey_required` | 0 | **1** |

Публичных обычных групп на проде теперь **ноль**; три публичных канала не
тронуты — для них это штатный режим.

**Что осталось и почему это не сделано мной.** Ротацию должен провести клиент:
новый ключ обязан родиться на устройстве админа, иначе сервер (или я) снова
знал бы его — то есть ровно то, от чего мы уходим. Группа помечена
`rekey_required = 1`, поэтому владельцу (`rmkhccc`) достаточно **зайти
админом в веб-клиент**: реализованный в RB-1 механизм проведёт ротацию сам и
раздаст новый ключ трём участникам. До этого момента группа не пишет —
это намеренная блокировка, а не поломка.

Мобильные сборки iOS и Android этот механизм ещё не содержат (код собран, но
приложения не переустановлены), поэтому ротацию нужно делать именно из веба.

---

Ниже — исходный план, по которому всё и делалось.

Порядок, который закрывает вопрос полностью:

1. **разпубличить** эту группу (`public_join = 0`, `join_key_b64 = NULL`) и
   предупредить трёх участников — либо, если публичность нужна по смыслу,
   превратить её в канал;
2. **сменить ключ** через `PUT /groups/{id}/key` из RB-1. Это важнее первого
   шага: снятие публичности само по себе ничего не отзывает — сервер держит
   этот ключ с 11.07.2026 и остаётся способен прочитать все 15 сообщений.
   Ротация как приватной группы раздаёт новый ключ по участникам, и сервер его
   уже не знает — E2E восстанавливается с этого момента;
3. **после этого** внести правку в `set_group_public`: требовать
   `is_channel = 1`. Тогда ломать будет нечего.

Прошлое остаётся скомпрометированным в любом случае: ключ у сервера был, и это
не отменяется задним числом.

### P2-1 · Токен WebSocket в query-строке — CONFIRMED, исправлено

`web/app.js` собирал `/ws?token=<30-дневный bearer>`. Такой URL попадает в
access-логи uvicorn и любого прокси перед ним, в `Referer`-цепочки, в панель
разработчика и в историю расширений. Добавлен `POST /ws/ticket`: одноразовый
билет с TTL 60 с, гасится DELETE…RETURNING при предъявлении, внутри ссылается на
ту же сессию (значит `/logout` и kick рвут соединение как раньше). Приём
`?token=` сохранён — установленные сборки iOS/Android обновятся не сразу.
`/logout` дополнительно чистит невыбранные билеты.

### P2-2 · Медиа без владельца, квоты и срока — CONFIRMED, исправлено частично

`/upload` писал файл на диск и возвращал `file_id`. Ни таблицы, ни владельца, ни
TTL, ни сборщика. `delete_group`, `delete_history` и `wipe_account` файлы не
трогали.

**MEDIA-ORPHAN-001:** загруженный и «не отправленный» файл живёт вечно.
**MEDIA-DELETE-002:** удаление истории/группы не удаляет медиа.
**Исчерпание диска:** 20 запросов/мин × 50 МБ = ~1 ГБ/мин с одного аккаунта,
без верхней границы. *DoS-проверка не проводилась — только чтение кода.*

**Важное ограничение, которое надо зафиксировать в документации:** refcount по
сообщениям **невозможен в принципе**. `file_id` лежит внутри зашифрованного
конверта, сервер его не видит. Любая retention-политика здесь может опираться
только на владельца и возраст.

Добавлено: таблица `uploads(file_id, user_id, kind, size_bytes, created_at)`,
квота на аккаунт (`AETHER_UPLOAD_QUOTA_MB`, дефолт 2048), TTL-сборщик
(`AETHER_MEDIA_TTL_DAYS`, **дефолт 0 = выключен**, чтобы апдейт не удалил
ничьи медиа без ведома владельца сервера), удаление файлов при
`POST /users/me/wipe`.

### P2-3 · В группу можно добавить несуществующего пользователя — CONFIRMED, исправлено

`add_group_member` вставлял `body.user_id` без проверки существования.
Призрак не виден в `GET /groups/{id}/members` (там `JOIN users`), но считается в
`member_count` и держит копию группового ключа. Добавлена проверка → 404.

### P2-4 · Форма публичного ключа аккаунта не проверялась — CONFIRMED, исправлено

`/users/register` и `PUT /users/me/public-key` проверяли только длину **строки**
(`Field(min_length=16, max_length=128)`). Ключ в 20 или 33 байта принимался и
падал позже 500-й ошибкой в `_wrap_key_for` (`PublicKey(...)`) при первом
вступлении в публичную группу. Добавлен `_validate_key_b64` (ровно 32 байта).

Отдельно отмечу, что для **prekey-директории** эта же гипотеза —
**FALSE POSITIVE**: `/keys/upload` уже проверяет и длину (32/64 байта), и
подписи по канонам `AETHER-IDKEY-1` / `AETHER-OTK-1` / `AETHER-DEVSIG-1`.

### P3-1 · Утечка WS-соединений — исправлено

`manager.disconnect()` висел только на `except WebSocketDisconnect`. Любое другое
исключение из `receive_text` оставляло мёртвый сокет в `active_connections`
навсегда. Перенесено в `finally`.

### P3-2 · Своё групповое сообщение возвращалось в inbox — исправлено

Ветка `inbox` c `since` не содержала `AND m.sender_id != user`, в отличие от
ветки без `since`. Автор получал собственный пост обратно; дедупликация целиком
ложилась на клиента. Ветки приведены к одному виду.

### Info · Что проверено и оказалось В ПОРЯДКЕ

Отдельно фиксирую, потому что «не нашёл» здесь важнее «нашёл»:

* **Перехват чужого крипто-устройства через `/keys/upload` невозможен.** Сессия
  может публиковать ключи под любым `device_id` своего аккаунта, но
  анти-даунгрейд (`signed_devices` как надгробие) и пиннинг мастер-ключа
  (`409 Master key mismatch`) не дают вору токена подменить identity чужого
  устройства: мастер выводится из приватного ключа аккаунта, которого у него нет.
  Это правильная и нетривиальная защита.
* **Ротация refresh-токенов честная.** Повторное предъявление потраченного
  токена гасит всю `family_id` и все выданные по ней сессии, отдельной
  транзакцией (иначе отзыв откатился бы вместе с 401). Комментарий в коде это
  объясняет — редкий случай, когда комментарий соответствует реализации.
* **Анти-вор гейт `kick_device`** считает возраст по `min(device_age,
  bind_age)` и применяется в том числе к «своему» устройству. Обход через
  перепривязку сессии закрыт.
* **Квота claim'ов OTK** (`otk_claims`, 5 на пару за 24 ч) с деградацией на
  fallback вместо отказа — корректная защита от выжигания чужих одноразовых.
* **Карантин нерасшифрованных сообщений в вебе существует**
  (`QUARANTINE_AFTER_MS`, `addUndecryptablePlaceholder`), то есть гипотеза
  «одно битое сообщение навсегда затыкает inbox на 200 записях» для веба —
  **FALSE POSITIVE**. Мелкое замечание: `undecryptableSince` живёт только в
  памяти вкладки, при перезагрузке таймер карантина обнуляется. Для Android и
  iOS аналог **не проверялся**.

---

## Результаты прогона (вторая сессия, 28.08.2026)

Стенд: macOS, Python 3.9.6 в venv, PostgreSQL из пакета `pgserver`, сервер
`uvicorn server.main:app --port 8099` на одноразовой БД. Режим регистрации
переключён на `OPEN` — свежий инстанс стартует в `APPROVAL`, и тесты, которые
заводят аккаунты, иначе получают `403 approval_required`. Это не дефект, но в
шапке `server/test_prekeys.py` рецепт неполный: про режим регистрации там
ничего не сказано, и первый же запуск упирается в 403.

| Проверка | Результат |
|---|---|
| `python3 -m compileall -q server` | **PASS** |
| `node --check web/app.js` | **PASS** |
| `cargo test --manifest-path core/Cargo.toml` | **PASS** — 54 теста ядра |
| `cargo test --test protocol_vectors` | **PASS** — 12 wire-векторов, 2 крипто-вектора |
| `server/test_server_info.py` | **PASS** — 11 проверок |
| `server/test_logout.py` | **PASS** |
| `server/test_prekeys.py` | **PASS** — 14 проверок |
| `server/test_security_regressions.py` | **PASS** — 8 из 8 (после правки самого теста, см. NEW-0) |
| `node web/test_wire.js` | **PASS** — 16 проверок |
| `node web/test_security.js` | **PASS** |

Ридер golden-векторов оказался рабочим с первой попытки: расхождения ядра с
`protocol_vectors/v1` нет. Обе гипотезы из плана проверки («ридер написан
неверно» / «ядро разошлось с векторами») — **не подтвердились**. Тесты не
вырожденные: оба падают, если набор векторов пуст или фильтр типов сломан
(`assert checked > 0`).

---

## Результаты прогона (третья сессия, 28.08.2026)

Стенд тот же, плюс появились `git` и Xcode 26.6 (build 17F113), которых в
прошлых сессиях не было. Закрыты все четыре пункта «осталось прогнать», кроме
запуска CI в самом GitHub Actions.

| Проверка | Результат |
|---|---|
| **Сборка iOS** (`xcodebuild -scheme AETHER -sdk iphonesimulator`) | **PASS** — BUILD SUCCEEDED |
| **Миграция `totp_pending_secret`** на доаудитной базе | **PASS** — 19 из 19 |
| **Сырой WS-хендшейк**, старый `?token=` и новый `?ticket=` | **PASS** — 7 из 7 |
| `cargo test` + `--all-features` | **PASS** — 54 теста ядра |
| `cargo test --test protocol_vectors` | **PASS** — 14 векторов |
| `server/test_server_info.py` | **PASS** |
| `server/test_logout.py` | **PASS** |
| `server/test_prekeys.py` | **PASS** |
| `server/test_security_regressions.py` | **PASS** — 8 из 8 |
| `node web/test_wire.js` / `test_security.js` | **PASS** |
| `compileall server` / `node --check web/app.js` | **PASS** |

Серверные наборы прогнаны повторно — на этот раз **не на свежей базе, а на
мигрированной**, то есть на том же состоянии, в которое приедет боевой сервер.

### Сборка iOS — NEW-1 закрыт

`ARCHS=arm64`. Первая попытка со стандартным `generic/platform=iOS Simulator`
падает на линковке, и это **не дефект правки**: в `ios/CoreFFI/SmCoreFFI.xcframework`
лежат только слайсы `ios-arm64` и `ios-arm64-simulator`, а `generic` просит ещё
и `x86_64`. Swift-фазы при этом проходят целиком — падает `Ld`.

### `iOS build` в Actions падал ДО этого аудита — три причины, все починены

Прогон в Actions показал, что мой прогноз был верен лишь отчасти: до линковки,
которую я предсказывал, дело даже не доходило, а после её починки вскрылась
третья причина. Итог — **`iOS build` зелёный впервые** (13m03s).

1. **Первым падает не линковка, а компиляция:** `cannot find 'Secrets' in
   scope`. `ios/AETHER/Core/Secrets.swift` держит адрес сервера и учётку TURN,
   лежит в `.gitignore` и в репозитории отсутствует — правильно, что
   отсутствует. Но CI его ниоткуда не брал, поэтому **каждый** запуск `ios.yml`
   падал, включая запуск на коммите `a8e1804`, до всех правок аудита. Локально
   сборка проходила только потому, что на машине владельца этот файл есть.
   Починено: шаг копирует `ios/Secrets.example.swift` в
   `AETHER/Core/Secrets.swift`, если настоящего нет. Значения-заглушки никуда
   не ходят, а набор символов у шаблона тот же — проверено локальной сборкой
   именно с шаблоном (`BUILD SUCCEEDED`).
2. **За ней шла бы линковка `x86_64`.** `build_core_ios.sh` собирает только
   `aarch64-apple-ios` и `aarch64-apple-ios-sim`, а `ios.yml` архитектуру не
   фиксировал. Починено: `ARCHS=arm64 ONLY_ACTIVE_ARCH=NO`. Заодно из шага
   `rustup target add` убран `x86_64-apple-ios` — скрипт его никогда не
   использовал.
3. **Ошибка компиляции, видимая только на Xcode 26.1.**
   `AetherMediaViewer.swift:94` — `Color.opacity` принимает `Double`,
   `dismissProgress` объявлен `CGFloat`, и выражение `1 - dismissProgress * 0.7`
   на 26.1.1 объявляется неоднозначным (компилятор не выбирает перегрузку `-`).
   Локальный Xcode 26.6 выводит тип сам, поэтому **локальная сборка была
   зелёной, а CI падал** — ровно тот случай, ради которого CI и нужен.
   Починено приведением к `Double`. Файл к правкам аудита отношения не имеет:
   последний раз менялся в `c986b9f`.

**Вывод, который стоит записать отдельно.** Локальная сборка iOS не является
доказательством того, что соберётся CI, и наоборот: у них разные версии Xcode
(26.6 против 26.1.1) и разное окружение (`Secrets.swift` есть только локально).
До этого аудита `iOS build` не проходил **ни разу**, и никто этого не замечал,
потому что локально всё собиралось.

Правка `bindCurrentDevice()` и её вызовы из `Session.activate()` и
`Session.signIn(paired:)` **компилируются**; вызываемые методы FFI
(`bindSessionDevice`, `bindSessionDeviceProof`) существуют в
`ios/AETHER/Core/Generated/sm_core.swift`.

### Миграция `totp_pending_secret` — 19 PASS

Проверялась не на свежей базе, а на **настоящей доаудитной**: поднят сервер из
коммита `a8e1804` (до правок), на нём заведены три состояния, затем на **той же
базе** запущен новый сервер — это и есть миграция.

Доаудитная схема подтверждена запросом к `information_schema`: в `users` были
только `totp_enabled` и `totp_secret`.

| Случай | Состояние до | Результат после миграции |
|---|---|---|
| ALICE | 2FA включена полностью | колонка добавлена, `totp_secret` не изменился, `totp_enabled=1`, `/2fa/status` = enabled, вход с кодом работает |
| BOB | настройка оборвана: секрет в `totp_secret`, `enabled=0` | compat-ветка нового `/2fa/enable` доводит настройку тем же секретом |
| CAROL | 2FA не трогали | не задета, вход без кода работает |

Отдельно подтверждено на мигрированной базе, что P0-фикс жив: `/2fa/setup` без
текущего кода → **403**, активный фактор на месте; с кодом → выдаётся
**кандидат** в `totp_pending_secret`, а `totp_secret` остаётся активным и вход
по старому коду продолжает работать до подтверждения.

Миграция **аддитивна и идемпотентна**: только `ADD COLUMN` под проверкой
`information_schema`, ничего не переписывается и не удаляется. В логе старта
нового сервера ошибок нет.

### Сырой WS-хендшейк — 7 PASS

Хендшейк слался руками по сокету (`GET /ws` + `Upgrade`), живость соединения
проверялась клиентским фреймингом `ping` → `pong`, а не одним лишь кодом 101.

| Тест | Ожидание | Факт |
|---|---|---|
| `WS-TOKEN-001` | старый `?token=` принимается | **101**, `ping→pong` — обратная совместимость установленных сборок цела |
| `WS-TOKEN-002` | мусорный токен отвергается | отказ |
| `WS-TOKEN-003` | без параметров отвергается | отказ |
| `WS-TOKEN-004` | токен истёкшей сессии отвергается | отказ |
| `WS-TICKET-003` | новый `?ticket=` даёт живое соединение | **101**, `ping→pong` |
| `WS-TICKET-001` | билет одноразовый | второй раз отказ |
| `WS-TICKET-002` | мусорный билет отвергается | отказ |

**Побочная находка (Info, не дефект безопасности).** Все отказы приходят как
**HTTP 403 до апгрейда**, а не как WS-close с кодом 1008: `await
websocket.close(code=1008)` до `accept()` не порождает close-фрейма, Starlette
просто не завершает апгрейд. Клиент, который различает причины отказа по
WS-close-коду, никакого 1008 не увидит — ему придёт обычный HTTP-статус. На
безопасность не влияет (отказ есть отказ), но диагностику на клиентах вводит в
заблуждение, и на это стоит смотреть, если ловить «почему WS не поднялся».

---

## Находки второй сессии

### NEW-0 · KEYSHAPE-001 ничего не проверял — дефект теста, исправлен

`server/test_security_regressions.py`, хелпер `register()` собирал тело как
`public_key_b64 or b64(...)`. Для случая «ключ длиной 0 байт» это даёт пустую
строку, она ложная, и вместо пустого ключа уходил валидный случайный —
регистрация проходила с 200, тест падал на собственной подмене.

Сервер вёл себя правильно с самого начала, проверено напрямую:

| Длина ключа | Ответ |
|---|---|
| 0 байт (пустая строка) | 422 `String should have at least 16 characters` (pydantic `min_length`) |
| 16 байт | 400 `public_key_b64 must encode 32 bytes` |
| 31 байт | 400 |
| **32 байта** | **200** |
| 33 байта | 400 |
| не-base64 | 400 `public_key_b64 must be base64url` |

Исправлено: сравнение с `None` вместо `or`, ожидаемый статус расширен до
`(400, 422)` — пустую строку отсекает pydantic, а не `_validate_key_b64`.

### NEW-1 · Пиннинг сессии ломает iOS после переустановки — CONFIRMED, исправлено

**Утверждение первой редакции отчёта и файла передачи было неверным.** Там
сказано: «`PUT /sessions/me/device` зовёт только веб; iOS и Android живут с
`sessions.device_id IS NULL`». На самом деле ручку зовут все трое:

* веб — `bindSessionDevice()`, `web/app.js`, один раз после логина;
* Android — `api.bindSessionDevice(myId, myDeviceId())` в `MessageRepository.start()`,
  на **каждом** старте и до `syncInbox()`, то есть переустановка со сменой
  `device_id` перепривязывает сессию сама;
* iOS — `api.bindSessionDeviceProof(...)` **только в `CoreClient.login()`**.

Отсюда дефект. На iOS токен лежит в Keychain и переживает удаление
приложения, а локальная база с `device_id` — не переживает. После
переустановки `Session.activate()` восстанавливает сессию из Keychain, минуя
`login()`, `myDeviceId()` резолвится в новый слот `ios-xxx` — и сессия,
запиненная к прежнему устройству, отвечает **403 на каждый `inbox` и `ack`**.
Перепривязать её некому: `activate()` и `signIn(paired:)` привязку не зовут.
Восстановление — только полный перелогин по паролю.

Тот же путь у связывания устройств: `signIn(paired:)` получает готовую сессию
и тоже не привязывается.

**Исправлено:** `CoreClient.bindCurrentDevice()` (привязка с Olm-identity, а
без Olm-аккаунта — без неё: привязка важнее доказательства, иначе не приедет
инбокс) и вызовы из `Session.activate()` и `Session.signIn(paired:)`.

### NEW-2 · Пиннинг обходится одной перепривязкой — CONFIRMED, не исправлено

Проверено на живом сервере, последовательность:

```
1) inbox?device_id=d1              → 200   (сессия пиннится к d1)
2) inbox?device_id=d2              → 403   device_id does not belong to this session
3) PUT /sessions/me/device {d2}    → 200   без identity_key_b64 вообще
4) inbox?device_id=d2              → 200   и отдаёт сообщение, адресованное d2
5) inbox?device_id=d1              → 403   пин переехал
```

`bind_session_device` перезаписывает пин безусловно; `identity_key_b64`
сверяется, только если он передан и устройство уже есть в `crypto_devices`.
Более того, перепривязка к **никогда не публиковавшемуся** устройству тоже
проходит (200).

Значит `_session_device()` — не граница безопасности, а гигиена: она чинит
исходный дефект (сессия одного устройства по ошибке подтверждала и навсегда
прятала чужие сообщения), но держатель токена по-прежнему читает инбокс
любого своего устройства, потратив один лишний запрос. Формулировку «сессия
одного устройства подтверждала сообщения другого» в описании P1-1 надо
понимать именно так: закрыт случайный путь, не злонамеренный.

Ужесточать нельзя без решения: запретить перепривязку — сломать iOS и
Android, которые ей и живут (см. NEW-1); требовать `identity_key_b64` всегда —
защита нулевая, ключ публичен и лежит в `GET /users/{id}/devices`
(это прямо написано в комментарии к `bind_session_device`).

**РЕШЕНИЕ ПРИНЯТО (владелец, 28.08.2026): оставить как гигиену.** Пиннинг
устройства официально **не является границей безопасности**. Ужесточение
отложено: challenge-response с подписью приватным ключом устройства — новая
крипто-поверхность в сервере и трёх клиентах, и она не окупается, пока
единственный выигрыш — закрыть доступ держателя токена к инбоксу его же
устройств.

Зафиксировано в коде, чтобы следующий не принял пиннинг за границу:
развёрнутый комментарий в `_session_device()` («ЭТО ГИГИЕНА, А НЕ ГРАНИЦА
БЕЗОПАСНОСТИ — осознанное решение, не недосмотр») и приписка в
`bind_session_device()` о том, что безусловная перепривязка оставлена
намеренно. README пиннинг границей нигде не объявлял — исправлять там нечего.

### NEW-3 · Идемпотентность по отпечатку конверта даёт ложный 409 — CONFIRMED, не исправлено

Файл передачи требовал проверить это «в бою». Условие подтверждено чтением
клиентов: **честный ретрай пересобирает конверт**.

`android/.../MessageRepository.kt`, `retryPendingCopies()`: перед повторной
отправкой заново зовётся `encryptDirectForDeviceLocked(...)`, то есть Ratchet
выдаёт другой шифротекст с тем же `client_id`. `_envelope_fingerprint` не
совпадёт → `409 Message id already in use` вместо `duplicate: true`.

Окно — обычный неоднозначный сбой: сервер сообщение записал, ответ до клиента
не доехал. Дальше Android жжёт `MAX_COPY_ATTEMPTS` попыток и выбрасывает
копию с предупреждением в лог, хотя сообщение получателю уже доставлено; у
отправителя оно при этом может остаться помеченным как неотправленное.

Существенное уточнение к дизайну: отпечаток конверта **в принципе не может**
поймать то, ради чего добавлен. И Ratchet, и AES-GCM групповых конвертов
недетерминированы — один и тот же текст даёт разные шифротексты. То есть
отпечаток не отличает «другой текст» от «тот же текст, перешифрованный», и
единственный его наблюдаемый эффект — ложные 409. Реальную защиту от исходного
дефекта («тот же UUID другому получателю») даёт сравнение
`sender_id` + `recipient_id` + `recipient_device_id`, оно остаётся в силе.

**Решение за владельцем**, два варианта:

* **A (дёшево, сейчас):** убрать отпечаток из сравнения, оставить тройку
  адресатов. Тогда падает случай «тот же `client_id`, тот же получатель и
  устройство, другой текст» — но клиенты выводят `client_id` детерминированно
  из id сообщения (`copyClientId(clientMsgId, deviceId)`), так что повтор с
  другим текстом означает баг клиента, а не штатный путь;
* **B (как предлагал план передачи):** отдельный столбец с идемпотентным
  ключом, который клиент считает **до** шифрования. Это правка протокола сразу
  в сервере и трёх клиентах.

**РЕШЕНИЕ ПРИНЯТО (владелец, 28.08.2026): вариант A — отпечаток убран.**
Исправлено и проверено в третьей сессии.

Сравнение в `send_message` теперь идёт по `sender_id` + `recipient_id` +
`recipient_device_id`; `_envelope_fingerprint()` удалена целиком, чтобы не
выглядеть действующей защитой. Правка только в сервере, клиенты не трогались.

`IDEMPOTENCY-001` переписан под новый контракт и **прогнан на живом сервере**:

* тот же `client_id` **другому получателю** → 409 (исходный дефект закрыт);
* побайтно тот же повтор → `duplicate: true`;
* ретрай с **перешифрованным** конвертом (тот же адресат, другой шифротекст) →
  `duplicate: true`. Раньше это был 409, из-за которого Android жёг попытки и
  выбрасывал уже доставленное сообщение.

Осознанная цена: случай «тот же `client_id`, тот же получатель и устройство,
другой текст» больше не ловится. Штатным путём он не возникает — клиенты
выводят `client_id` детерминированно из id сообщения
(`copyClientId(clientMsgId, deviceId)`), так что это признак бага клиента.

### NEW-4 · `/avatars` не проверяет квоту

`upload_file` проверяет `_used_bytes(current_user) >= UPLOAD_QUOTA_BYTES`,
`upload_avatar` — нет, хотя пишет строку в ту же таблицу и попадает в ту же
сумму (`_used_bytes` не фильтрует по `kind`). То есть аккаунт, упёршийся в
квоту, продолжает грузить аватарки: 10 запросов в минуту по 5 МБ. И
`gc_expired_uploads` аватарки сознательно не трогает.

Мелочь того же ряда: квота проверяется **до** сохранения, поэтому реальный
потолок — `UPLOAD_QUOTA_BYTES + MAX_UPLOAD_BYTES` (50 МБ сверху).

### NEW-5 · Осиротевшие медиа: решение всё ещё нужно

Подтверждено на рабочем дереве: в `server/uploads` лежит файл, которому
неоткуда взяться в таблице `uploads` — она заполняется только новыми
загрузками. Такие файлы не считаются в квоту и не удаляются TTL-сборщиком
(который к тому же выключен по умолчанию, `AETHER_MEDIA_TTL_DAYS=0`).
Разовый скрипт учёта существующих файлов **не написан** — это решение
владельца, как и было записано в плане передачи.

### NEW-6 · Веб мог сменить `device_id` в живой сессии — исправлено

`ensureRatchetKeys()` умеет посреди работы перевести веб с `primary` на новое
`web-xxxx` (когда `primary` занял телефон с другим identity), а зовётся она в
том числе после логина — при доборе одноразовых ключей. С пиннингом это
означало бы 403 на inbox до конца сессии, потому что `bindSessionDevice()`
вызывается ровно один раз. Добавлен вызов привязки сразу после смены
`device_id`.

---

## Проверка гипотез из ТЗ (раздел 26)

| # | Гипотеза | Вердикт |
|---|---|---|
| 1 | Android на `main` шлёт legacy crypto_box для DM | **CONFIRMED** (третья сессия, по исходникам ветки `main`). В `android/` на `main` слово «ratchet» не встречается **ни разу**; `MessageRepository.kt` прямым текстом описывает путь: «личный чат → crypto_box на ключ собеседника (TOFU-пиннинг)» и «Групповой конверт — AES-GCM общим ключом; личный — crypto_box». На `android-parity` в том же файле 23 упоминания Ratchet, а crypto_box из DM-пути ушёл (остался только в `CreateGroupScreen`/`PairDeviceScreen`). Практическое следствие мягче, чем звучит: сервер отвергает не-Ratchet личку (`ALLOW_LEGACY_DIRECT` по умолчанию выключен), поэтому сборка с `main` личное сообщение **не отправит вообще**, а не отправит его небезопасно |
| 2 | Свежие Android-ветки уже с Ratchet, release-ветка не `main` | **ПОДТВЕРЖДЕНО** — рабочее дерево на `android-parity`, Ratchet есть; `main` отстаёт на 159 коммитов и Ratchet не содержит |
| 3 | `/2fa/setup` снимает 2FA | **CONFIRMED**, исправлено (P0-1) |
| 4 | ACK за чужое устройство | **CONFIRMED**, исправлено (P1-1), проверено runtime'ом (`DEVICE-ACK-001`). Границей безопасности не является — см. NEW-2 |
| 5 | inbox доверяет `device_id` | **CONFIRMED**, исправлено (P1-1), проверено runtime'ом (`DEVICE-INBOX-002`). См. NEW-1 (ломало iOS) и NEW-2 |
| 6 | `target_device_id` не проверяется | **CONFIRMED**, исправлено (P1-2) |
| 7 | Дубликат `client_id` только по sender | **CONFIRMED**, исправлено (P1-3), проверено runtime'ом (`IDEMPOTENCY-001`). Но выбранная форма проверки даёт ложный 409 на честном ретрае — см. NEW-3 |
| 8 | Нет ротации ключа при remove/leave | **CONFIRMED**, НЕ исправлено (P1-4) |
| 9 | Публичная обычная группа отдаёт ключ серверу | **CONFIRMED**, НЕ исправлено (P1-5) |
| 10 | `/upload` без owner/refcount/TTL | **CONFIRMED**, исправлено частично (P2-2). Refcount невозможен по устройству протокола |
| 11 | WS-токен в query | **CONFIRMED**, исправлено (P2-1) |
| 12 | Security-доки описывают устаревшее состояние | **CONFIRMED** — см. DOCUMENTATION DRIFT |
| 13 | CI гоняет только сборку iOS | **CONFIRMED** — в `.github/workflows` был ровно один файл, `ios.yml`. Исправлено: добавлен `ci.yml` |

---

## СОСТОЯНИЕ ВЕТОК

**Заполнено в третьей сессии.** В прошлых сессиях `git` был недоступен, и
таблица стояла пустой. Дополнительно выяснилось, что клон был **shallow**
(`.git/shallow` с тремя граничными коммитами) — из-за этого прошлые выводы о
ветках строились на обрезанной истории. История дотянута
(`git fetch --unshallow`), цифры ниже настоящие.

Ahead/behind считаются относительно `origin/main` (`567ef19`).

| Branch | SHA | Дата | Behind | Ahead | Примечание |
|---|---|---|---|---|---|
| `android-parity` (локальный) | `f0b761c` | 28.08.2026 | 110 | 159 | **рабочее дерево, HEAD**; +1 к origin — коммит этого аудита |
| `origin/android-parity` | `a8e1804` | 27.08.2026 | 110 | 157 | |
| `multi-server` | `af26a28` | 26.08.2026 | 110 | 137 | локальная, remote-аналога нет |
| `web-secure` (локальный) | `7be148c` | 25.08.2026 | 110 | 112 | = локальный `main` + 2 коммита |
| `main` (локальный) | `c986b9f` | 23.08.2026 | 110 | 110 | **дерево идентично `origin/main`** |
| `origin/main` | `567ef19` | 23.08.2026 | — | — | |
| `origin/web-secure` | `567ef19` | 23.08.2026 | 0 | 0 | тот же коммит, что `origin/main` |
| `round-ui-performance` | `0449110` | 22.08.2026 | 111 | 74 | **другой корневой коммит** |
| `origin/round-ui-performance` | `ab1b93e` | 22.08.2026 | 111 | 74 | |
| `android` | `ca98b0c` | 19.07.2026 | 110 | 33 | старая, до Ratchet |
| `origin/android` | `124977c` | 19.07.2026 | 81 | 4 | |

Три факта, которых раньше не было видно:

1. **История `main` переписана.** Локальный `main` (`c986b9f`) и `origin/main`
   (`567ef19`) расходятся на 110 коммитов в каждую сторону, общий предок —
   только корневой коммит `7791c9b`. При этом **дерево у них побайтово
   одинаковое** (`3709989…`): содержимое то же, родословная разная. То есть
   кто-то пересобрал историю заново, а не потерял правки.
2. **`origin/web-secure` — это ровно `origin/main`** (0/0). Ветка, судя по
   имени отвечавшая за безопасность веба, на удалёнке уже слита/сброшена
   в main.
3. **`round-ui-performance` растёт из другого корня** (`cbfbfc5`), общего
   предка с `origin/main` нет вообще. Слить её обычным merge не выйдет.

**Какая комбинация актуальна — ОДНОЗНАЧНОГО ОТВЕТА ПО-ПРЕЖНЕМУ НЕТ**, и теперь
понятно почему: `main` на удалёнке содержит то же, что локальный, но с чужой
родословной, а все правки этого аудита и весь Ratchet лежат на
`android-parity`, который на 159 коммитов впереди. Это **отдельный Release
Blocker процессного характера**: перед релизом надо привести `main` к
состоянию, которое реально собирается и тестируется, и записать это в README
вместо нынешних инструкций.

---

## CROSS-CLIENT MATRIX

**Живых сценариев клиент↔клиент по-прежнему нет** — для них нужны два
физических устройства. Но вторая сессия закрыла общий слой, на котором эта
совместимость держится: golden-векторы.

| Уровень | Чем проверено | Результат |
|---|---|---|
| wire-формат (`text`, `reply`, `reaction`, `edit`, `delete`, `read`, `delivered`, `media:*`, `group_payload`) | `cargo test --test protocol_vectors`, 12 векторов | **PASS** — `wire_decode`/`wire_encode` ядра совпадают с `expected_encoded_json` посемантически, и `decode(encode(x)) == decode(x)` |
| AES-256-GCM (медиа, групповые конверты) | тот же ридер, `deterministic_crypto` | **PASS** — шифротекст из вектора расшифрован ядром побайтно |
| crypto_box (curve25519xsalsa20poly1305) | тот же ридер | **PASS** |
| веб ↔ Android по wire-адаптеру | `node web/test_wire.js`, 16 кейсов, включая явные «android reaction → web» и порядок тега AES-GCM | **PASS** |
| iOS-читатель векторов | `ios/check_wire_compat.swift` существует, но в этой сессии **не собирался** | NOT RUN |
| Android-читатель векторов | **не существует** — ни `androidTest`, ни ассетов | НЕТ |
| Web↔Android / Web↔iOS / Android↔iOS живая переписка | — | **NOT RUN**, нужны устройства |

То есть Rust-ядро и веб теперь доказанно говорят на одном формате. Дырка в
покрытии сместилась: она не в «векторы декоративны» (это исправлено), а в том,
что Android их не читает вообще, а iOS-читатель не запускается в CI.

---

## DOCUMENTATION DRIFT

| Утверждение | Где сказано | Что в коде | Верно? |
|---|---|---|---|
| Сервер знает ключ только у публичных **каналов** | `README.md`, «Philosophy» | `set_group_public` не проверяет `is_channel` | **НЕТ** (P1-5) |
| `cargo test --manifest-path core/Cargo.toml --test protocol_vectors` | `protocol_vectors/v1/README.md` | цели `tests/protocol_vectors.rs` **не существовало** | **НЕТ** — исправлено, ридер написан и **прогнан зелёным**: 12 wire-векторов и 2 крипто-вектора |
| `node web/test_wire.js` читает векторы | там же | `test_wire.js` не упоминает `protocol_vectors` вообще, у него свои inline-кейсы | **НЕТ** |
| Android-инструментация пакует векторы как test assets | там же | в `android/` нет ни `androidTest`, ни ассетов с векторами | **НЕТ** |
| `swiftc ios/AETHER/Core/Wire.swift ios/check_wire_compat.swift` | там же | файл существует | **ДА** |
| Сервер хранит сообщения только до доставки | `README.md` | верно для `messages`, **неверно для медиа**: файлы жили вечно | было НЕТ, исправлено частично |

То есть из четырёх заявленных читателей golden-векторов реально существовал
один. Набор векторов был декоративным: изменение одного клиента ничего бы не
уронило.

---

## CI / TEST COVERAGE GAPS

Было: единственный workflow `ios.yml` — сборка iOS. Не запускалось **ничего**
из уже написанного: ни `cargo test`, ни `web/test_wire.js`, ни
`web/test_security.js`, ни один из шести серверных смоук-тестов.

Добавлен `.github/workflows/ci.yml` с тремя job'ами: Rust core + golden-векторы;
web (`node --check`, wire, security); server на одноразовом PostgreSQL 16 с
прогоном `test_server_info`, `test_logout`, `test_prekeys` и новых регрессий.

**Правка второй сессии:** в job'е `server` не хватало шага, открывающего
регистрацию. Свежий инстанс стартует в `APPROVAL`, и весь job упал бы на
`test_logout` с `403 approval_required` — ровно на этом упёрся локальный
прогон. Добавлен шаг `Открыть регистрацию` (`UPDATE server_meta ... 'OPEN'`)
сразу после подъёма сервера: `server_meta` создаётся на старте, поэтому раньше
его ставить нельзя. Сам workflow **в GitHub Actions не запускался** — тот же
набор команд воспроизведён локально и зелёный.

**Остаётся не покрытым:** Android (нет ни compile-job, ни unit-тестов, ни
инструментальных), iOS-тесты (есть только сборка), cross-client conformance
между Rust ↔ Android ↔ iOS ↔ Web на общих векторах.

---

## Что сделано в коде

**`server/main.py`**
* `totp_pending_secret` + переписанные `/2fa/setup` и `/2fa/enable` (P0-1);
* `_bearer_token()`, `_session_device()`; применены в `/messages/ack` и
  `/messages/inbox` (P1-1);
* проверка `target_device_id` по `crypto_devices` получателя (P1-2);
* `_envelope_fingerprint()` и полноценная проверка дубликата (P1-3);
* проверка существования пользователя в `add_group_member` (P2-3);
* `_validate_key_b64` в `/users/register` и `PUT /users/me/public-key` (P2-4);
* таблица `uploads`, квота, `gc_expired_uploads()` + фоновый цикл, удаление
  медиа при wipe (P2-2);
* `POST /ws/ticket`, приём `?ticket=` в `/ws`, чистка билетов в `/logout` (P2-1);
* `manager.disconnect()` в `finally` (P3-1);
* выравнена ветка `inbox` c `since` (P3-2);
* таблица `ws_tickets`.

**`web/app.js`**
* `wsTicket()` + `wsUrlFromServer(ticket)`, с откатом на `?token=` для старого
  сервера;
* привязка сессии к устройству сразу после смены `device_id` в
  `ensureRatchetKeys()` (NEW-6).

**`ios/AETHER/Core/CoreClient.swift`, `ios/AETHER/Core/Session.swift`** —
`bindCurrentDevice()` и его вызовы из `Session.activate()` и
`Session.signIn(paired:)`: сессия, восстановленная из Keychain или полученная
при связывании устройств, теперь объявляет своё устройство, а не упирается в
403 после переустановки (NEW-1). **Не скомпилировано** — сборка iOS в этой
сессии была недоступна.

**Новые файлы**
* `server/test_security_regressions.py` — 8 регрессий на всё исправленное
  (прогнаны, 8/8 зелёные);
* `core/tests/protocol_vectors.rs` — недостающий ридер golden-векторов
  (wire round-trip + расшифровка deterministic-векторов), прогнан;
* `.github/workflows/ci.yml`.

---

## TOP 10 FIXES по приоритету

0. ~~Прогнать всё написанное~~ — **сделано**, см. «Результаты прогона».
1. **Собрать и прогнать iOS с правкой NEW-1.** Она написана, но не
   компилировалась. Без неё переустановленный iOS не получает сообщений вообще,
   и это хуже исходного дефекта, который правка закрывает.
2. **RB-1: ротация группового ключа.** Дизайн выше. Затрагивает server + 3
   клиента. Риск регрессии высокий — нужна эпоха, иначе старые клиенты будут
   слать на устаревшем ключе. Тест: `GROUP-REMOVE-001` с криптографической
   проверкой, что удалённый не вскрывает новый шифротекст.
3. **RB-2: решить судьбу публичных групп** (вариант A или B).
3a. **NEW-3: решить форму идемпотентности** (убрать отпечаток конверта либо
   ввести идемпотентный ключ, считаемый до шифрования).
3b. **NEW-2: решить, чем должен быть пиннинг устройства** — гигиеной, как
   сейчас, или границей; во втором случае нужен способ доказать владение
   устройством, которого публичный `identity_key_b64` не даёт.
4. **Привести `main` в порядок** и зафиксировать релизную комбинацию веток.
5. **Android в CI**: хотя бы compile + unit + чтение общих векторов.
6. **Cross-client conformance suite** на `protocol_vectors/v1` — сейчас векторы
   читает только Rust (после этого коммита) и iOS.
7. **Обновить `protocol_vectors/v1/README.md`** — убрать несуществующих
   читателей или написать их.
8. **Обновить README** про retention медиа и про публичные группы.
9. **Перевести iOS и Android на `POST /ws/ticket`** — сейчас они остаются на
   `?token=`, и приём токена в query придётся держать, пока они не обновятся.
10. **Проверить карантин нерасшифрованных сообщений на Android и iOS** —
    в вебе он есть, для остальных не проверялся.

---

## Как проверить написанное

Рецепт, по которому прогон реально делался. Шаг с режимом регистрации
обязателен — без него всё упрётся в `403 approval_required`.

```bash
python3 -m compileall -q server && node --check web/app.js
```

```bash
cargo test --manifest-path core/Cargo.toml
```

```bash
pip install pgserver python-multipart -r requirements.txt
```

```bash
python -c "import pgserver; print(pgserver.get_server('/tmp/aether-pg', cleanup_mode=None).get_uri())"
```

```bash
DB_NAME=postgres DB_USER=postgres DB_PASS= DB_HOST=<каталог-сокета> uvicorn server.main:app --port 8099
```

Свежий инстанс стартует в `APPROVAL`. Переключить в БД:

```bash
psql -h <каталог-сокета> -U postgres -c "UPDATE server_meta SET value='OPEN' WHERE key='aether.registration_mode'"
```

```bash
AETHER_URL=http://127.0.0.1:8099 python3 server/test_server_info.py
```

```bash
AETHER_URL=http://127.0.0.1:8099 python3 server/test_logout.py
```

```bash
AETHER_URL=http://127.0.0.1:8099 python3 server/test_prekeys.py
```

```bash
AETHER_URL=http://127.0.0.1:8099 python3 server/test_security_regressions.py
```

```bash
node web/test_wire.js && node web/test_security.js
```

Регресс-набор создаёт больше десятка аккаунтов и упирается в лимит
15 запросов/мин на `/users/register` и `/users/login`; он сам ждёт окно и
повторяет, поэтому полный прогон занимает пару минут — это не зависание.

---

## Что я реально проверил / не смог

**Проверил чтением кода (первая сессия):** весь `server/main.py` (3020 строк
до правок), схему БД и миграции, prekey-директорию и подписи,
сессии/refresh/kick, группы и каналы, публичное вступление, медиа, WebSocket;
в вебе — путь ack/inbox, подключение WS, обработку нерасшифрованных сообщений;
наличие Ratchet-пути во всех трёх клиентах; `.github/workflows`;
`protocol_vectors/v1` и его читателей.

**Реально запустил (вторая сессия):** всё из таблицы «Результаты прогона» —
компиляцию сервера и веба, 56 тестов ядра, четыре серверных смоук-набора
против живого PostgreSQL, два веб-набора. Плюс адресные пробы на живом сервере:
форма публичного ключа (шесть длин), полная последовательность пиннинга
устройства и его обхода перепривязкой (NEW-2).

**Реально запустил (третья сессия):** сборку iOS (`BUILD SUCCEEDED`, arm64);
миграцию `totp_pending_secret` на доаудитной базе через реальный апгрейд
сервера (19 проверок); сырой WS-хендшейк по `?token=` и `?ticket=` на сокетах
(7 проверок); повторный прогон всех серверных и веб-наборов на **мигрированной**
базе; сравнение веток по настоящей истории после `git fetch --unshallow`;
гипотезу №1 по исходникам ветки `main`.

**Не смог проверить:**

* **сборку и тесты Android** — Android SDK в этой сессии не поднимался.
  Гипотеза №1 закрыта чтением исходников `main`, но сборка не проверялась;
* — (закрыто) **CI в GitHub Actions прогнан.** Ветка запушена, `ci.yml`
  отработал в Actions: `Rust core + protocol vectors`, `Web (wire + security)`
  и `Server API + security regressions` — **все три зелёные**, 2m53s.
  Шаг «Открыть регистрацию» сработал: `403 approval_required` не возникло;
* живые сценарии клиент↔клиент; локальные хранилища Android и iOS
  (Keystore/Keychain, SQLCipher, состояние Ratchet); TOFU и смену identity на
  клиентах; offline/outbox и network chaos; WebRTC-сигналинг под нагрузкой;
  поведение при >200 сообщениях в inbox;
* **поведение iOS-правки в рантайме** — `bindCurrentDevice()` компилируется, но
  сценарий «переустановил приложение → инбокс приехал» требует устройства и
  здесь не воспроизводился.

**Требует двух физических устройств:** DEVICE-KICK-004, fan-out на A1/A2/A3,
glare-сценарии Olm.
**Требует iPhone/APNs:** доставку пушей, VoIP/CallKit, `_apns_notify_offline`.
**Требует Android:** проверку legacy crypto_box на `main`, Room/SQLCipher,
карантин нерасшифрованных.
**Требует staging-сервера:** все MEDIA-*, IDEMPOTENCY-*, DEVICE-*, 2FA-*,
GROUP-REMOVE-001 и оценку исчерпания диска.
