fastapi>=0.110
uvicorn>=0.27
websockets>=12.0  # ОБЯЗАТЕЛЬНО: без него uvicorn не обслуживает /ws → звонки/typing не работают (404 на upgrade)
pydantic>=2.0
requests>=2.31
PyNaCl>=1.5
cryptography>=42.0
psycopg2-binary==2.9.9
slowapi>=0.1.9
# Argon2id для паролей. PBKDF2 остаётся только для ПРОВЕРКИ старых хешей —
# новые не создаются (server/password_policy.py).
argon2-cffi>=23.1
# ОБЯЗАТЕЛЬНО: /upload и /avatars объявлены как UploadFile=File(...), а FastAPI
# проверяет multipart на импорте — без пакета приложение не стартует вообще.
python-multipart>=0.0.9
# APNs-пуши (опционально: без APNS_* переменных окружения не используются)
PyJWT>=2.8
httpx[http2]>=0.27
